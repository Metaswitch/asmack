Built in ubuntu with the following command

./build.bash -b xep0198 -r /home/ubuntu/work/smack/

Then completed in Mac OSX with the following command

ant -Dandroid.version=android-10 compile-android
